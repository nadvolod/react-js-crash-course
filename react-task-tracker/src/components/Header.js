// With the extension installed, running 'rafce' will create a new component
const Header = () => {
    return (
        <header>
            <h1>Task Tracker</h1>
        </header>
    )
}

export default Header

